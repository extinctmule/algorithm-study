# odict = {"A":2}
# print(list(odict)[0])



'''
외울 것:
arpha[ord(ch)-ord('a')]
이런식으로 씀, 문자 하나를 그에해당하는 유니코드번지수로 바꿈.
chr(97)은 반대로, 97을 'a'로 바꿔줌


string에는 sort함수가 없어서 쓸 수 없고,
s = ''.join(sorted(s)) 하면 사전 순으로 정렬된 스트링을 얻을 수 있다.
'''